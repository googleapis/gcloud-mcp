# Invoked via: /gcloud:filtering_context

prompt = """
    When using `run_gcloud_command`: 
        1. Always use `--format="json"`. 
        2. Use `--filter` to restrict results, prioritizing `:` for pattern matching and never quoting the right side of the colon in filters. 
        3. Access nested data directly with projections and use `.basename()` for URL fields. 
        4. Retrieve only necessary information for the user intent. Utilize projection capability of `--format` reduce data size.
        5. If the exact JSON key path for filtering is unknown, run `gcloud ... list --limit=1 --format=json` to discover it.  
    """
description = "To inject additional gcloud global flag settings into the current context"